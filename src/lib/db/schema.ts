// Blank schema. Add your tables here, then run `npm run db:generate`
// to create a migration in drizzle/ and commit it, then apply
// drizzle/meta/_journal.json migrations to a fresh D1 on deploy.
//
// Example:
//   import { integer, sqliteTable, text } from "drizzle-orm/sqlite-core";
//   export const notes = sqliteTable("notes", {
//     id: text("id").primaryKey(),
//     body: text("body").notNull(),
//   });

export {};
